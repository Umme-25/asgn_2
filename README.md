# asgn_2
  ASSIGNMENT 2

      TASK 1
      
          Taken input from user and converted to integer
             Performed operation by if and else conditions
             Condition given is %2 to check the divisibility of number
           if block is used for even numbers -> means number is divisible by 2
           else block is used for odd numbers -> means the numer is not divisible by 2
         Displayed result according to the condition which is TRUE.


     TASK 2
          A variable sum with value zero
             then used for loop with Range function from 1 to 51 
             that takes upto 50 only and added all numbers one by one and stored in sum variable
          Displayed the result using f-sting in print function.
